/** @type {const} */
const themeColors = {
  primary:    { light: '#C4A4D4', dark: '#C9AED6' },   // Soft lavender
  background: { light: '#FBF7F4', dark: '#2A2520' },   // Warm off-white / warm dark brown
  surface:    { light: '#F5EDE6', dark: '#3A332D' },   // Light warm beige / warm dark surface
  foreground: { light: '#4A3F3A', dark: '#E8DDD4' },   // Warm dark brown / warm light text
  muted:      { light: '#A89B91', dark: '#9B8E84' },   // Warm taupe
  border:     { light: '#E8DDD4', dark: '#4A4038' },   // Soft sand
  success:    { light: '#A8C5A0', dark: '#A8C5A0' },   // Muted sage green
  warning:    { light: '#E8C9A0', dark: '#E8C9A0' },   // Warm peach
  error:      { light: '#D4A0A0', dark: '#D4A0A0' },   // Dusty rose — never alarming
};

module.exports = { themeColors };
